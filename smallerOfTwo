function smallerOfTwo(a, b){
let number = 0;
let index = 0;
if (a < b){
number = number + a;
index = index + 1;
} if (a > b){
number = number + b;
index = index + 2;
} if (a === b){
number = number + a;
index = index + 1;
}
return [number, index];
}
